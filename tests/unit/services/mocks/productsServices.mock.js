const validName = 'Martelo de Thor'

const productSearchById = {
  id: 1,
  name: validName
}

const invalidName = 'a';

const updateIdMock = {
  name: 'Novo nome'
}

const updatedMock = {
  id: 1,
  name: 'Novo nome'
}

module.exports = {
  productSearchById,
  validName,
  invalidName,
  updateIdMock,
  updatedMock,
}